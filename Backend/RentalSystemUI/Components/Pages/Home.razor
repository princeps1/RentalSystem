@page "/"


<section class="hero">
    <h1>Welcome</h1>
    <p>Lorem sint harum, rem reprehenderit ad iusto, laborum minus voluptatibus deleniti aliquam suscipit vitae!</p>
    <video preload="auto" muted autoplay loop playsinline>
        <source src="videos/auto video.mp4" type="video/mp4">
    </video>
</section>






















@* @inject IKorisnik KorisnikData;
@using RentalSystemUI.DTOs.Korisnik

@if (korisnici is not null)
{
    @foreach(var korisnik in korisnici)
    {
        <li>@korisnik.ImePrezime @korisnik.JMBG</li>
    }
}

@code {
    private List<KorisnikDTO>? korisnici;
    protected override async Task OnInitializedAsync()
    {
        korisnici = await KorisnikData.PrikaziSve();
    }
} *@
